version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: khazane-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: khazane
      POSTGRES_PASSWORD: khazane_secret
      POSTGRES_DB: khazane_db
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: khazane-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
